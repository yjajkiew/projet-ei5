#############################
# Todo List for REST server #
#############################

1 - 'WEB' LAYER
	[X] -> Find a solution to get parameters from the 'POST' cmd in raw JSON (using different POST app in Chrome)
	[X] -> arduino answer not displayed, even if error ? (used callback functions)
	[ ] -> Callback not working for arduinos list, only return is working ??????? wtf ???


2 - 'METIER' LAYER
	[X] -> find why JSON object aren't correclty parsed (Error in variables name in web.js.....)
	[X] -> find why callback function doesn't work for read / write function (error in the 'WEB' layer, call to function wasn't good)
	[ ] -> handle exceptions
	[ ] -> check data from the 'POST' cmd
	[ ] -> change JSON return trame


3 - 'DAO' LAYER
	[X] -> succesfully send a request and get correct answer from arduino (blink) (in daoClient_test.js)
	[ ] -> get arudino IP / PORT automatically from the arduino table & arduino ID
	[ ] -> regroup error handling in an external function 
	[X] -> check if arduino already in list befor adding 
	[ ] -> check perdiodicaly if listed arduinos are still connected
	[ ] -> check data from arduino : good formating, security issue ?
	[ ] -> check if port is correclty displayed when new arduino connect (1025 -> 102 ????)
	[ ] -> put server & client in other files ?

4 - 'UTILITIES'
	[ ] -> Arduino info persitency : save states in DB ?